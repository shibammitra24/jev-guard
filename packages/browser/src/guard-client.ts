import type { GuardedBrowserRequest, GuardedBrowserResponse } from 'jev-core';
export async function guardBrowserAction(endpoint: string, token: string, request: GuardedBrowserRequest, fetcher: typeof fetch = fetch): Promise<GuardedBrowserResponse> { const response = await fetcher(`${endpoint.replace(/\/$/, '')}/v1/decide`, { method: 'POST', headers: { authorization: `Bearer ${token}`, 'content-type': 'application/json' }, body: JSON.stringify(request) }); if (!response.ok) throw new Error(`Guard daemon rejected browser action (${response.status})`); return response.json() as Promise<GuardedBrowserResponse>; }
